# Personal Hyprland binding overrides
# This file contains jimmitjoo's custom keybindings

# Use Super + Q to close windows (instead of Super + W)
unbind = SUPER, W
bindd = SUPER, Q, Close window, killactive,
